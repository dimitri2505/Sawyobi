# Local PGlite (default — in-process Postgres, zero install)
DATABASE_URL=pglite://./data/pgdata

# To use Neon Postgres instead, comment out the line above and use:
# DATABASE_URL=postgresql://user:password@host/db?sslmode=require
